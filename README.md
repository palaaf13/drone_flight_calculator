## AI-Use Disclosure

Used GitHub Copilot and GitHub Copilot Chat (/tests) to assist with implementing `calculate_flight_time()` and `flight_time_table()` and to generate the initial pytest test skeletons. Edited Copilot's suggestions where needed, including correcting the negative-weight error message and reviewing the generated tests. Personally verified the calculation logic and confirmed all tests pass with pytest.
